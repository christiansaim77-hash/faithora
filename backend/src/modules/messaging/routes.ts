/**
 * FAITHORA Messaging Routes
 * Handles chat and messaging endpoints.
 */

export const messagingRoutes = {
  send: "/send",
  conversations: "/conversations",
  messages: "/:conversationId/messages"
};